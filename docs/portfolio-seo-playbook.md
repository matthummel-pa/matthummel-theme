# Portfolio and local SEO playbook

Cursor loads this as `.cursor/rules/portfolio-seo-playbook.mdc` (always on). That file maps the playbook onto this Sage theme. Follow the `.mdc` when they disagree.

Source notes Matt pasted: dual audience (shops vs developers), Gettysburg keyword frames, a hook / proof / code layout for long write-ups, and hard caps on titles (60), meta (155), city mentions (twice on local pages), and 3-sentence paragraphs.

Do not install ACF, invent testimonials, or hardcode a second title stack. Work stays example sites on `/projects/`.
